__pycache__
*.pyc
*.mnc
*.mnc.gz

# ...except the test volumes, which are checked in (see tests/data/README.md)
!/tests/data/*.mnc

# Vendored dependencies, each with its own git repository
/legacy/
/minc2-simple/

# The colin27 download experiments/ unpacks into experiments/data/, which is
# not checked in either
/mni_colin27_1998_minc2.zip

# Build products of torch_n3/_legacy/build_legacy.py
/torch_n3/_legacy/build/
*.o
*.so
_n3legacy.cc

.pytest_cache
